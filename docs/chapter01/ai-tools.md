# 你的 AI 编程搭档

!!! quote "从 Coding 到 Commanding"
    在过去，程序员的核心能力是记忆语法和 API；在 AI 时代，程序员的核心能力变成了 **定义问题** 和 **审查代码**。
    
    本课程要求你配备两类 AI 助手：**对话型军师**（如 DeepSeek）和 **沉浸式副驾**（如通义灵码）。

---

## 🤖 我们的 AI 工具箱

根据教学大纲要求，本课程推荐以下组合：

### 1. 对话型军师 (Web/App 端)
用于架构设计、思路探讨、复杂原理讲解。

* **DeepSeek (深度求索)**：**[强烈推荐]** 国产之光，逻辑推理能力极强，后续课程中的 MCP 和 Tool Calling 环节将重点使用它。
* **豆包 / Kimi**：响应速度快，适合处理长文档阅读和日常代码片段生成。

### 2. 沉浸式副驾 (IDE 插件端)
用于在 IntelliJ IDEA 中实时生成代码、写注释、补全逻辑。

* **通义灵码 (Tongyi Lingma)**：**[必装]** 阿里出品，与我们使用的 Dragonwell JDK 同源，对 Java 生态支持极佳。它可以帮你写单元测试、解释代码、自动生成 Commit 信息。
* **Trae / Cursor**：新兴的 AI 原生编辑器（选装），适合体验极致的“全自动编程”。

---

## 🗣️ 核心技能：Prompt 工程入门

AI 不是算命先生，它需要你给出清晰的指令。一个高质量的 Prompt（提示词）通常包含四个要素：**角色 + 背景 + 任务 + 约束**。

=== "❌ 错误的提问"
    > “帮我写个登录。”
    
    *AI 的困惑：是用 Java 还是 Python？是控制台登录还是 Web 登录？要连数据库吗？*

=== "✅ 正确的提问 (Prompt)"
    > **(角色)** 你是一个资深的 Java Spring Boot 开发专家。
    > **(背景)** 我正在开发一个基于 Spring Boot 3.x 的后端系统，使用 MyBatis 操作 openGauss 数据库。
    > **(任务)** 请帮我设计一个用户登录接口。
    > **(约束)** > 1. 使用 RESTful 风格。
    > 2. 密码需要进行 MD5 加密比对。
    > 3. 返回结果需封装在统一的 `Result<T>` 类中。
    > 4. 请给出 Controller 和 Service 层代码。

!!! tip "作业考核点"
    记得保存你与 AI 的精彩对话截图！这是期末平时成绩中 **“AI 应用报告”** 的重要素材。

---

## 🛠️ 实战场景演练

在接下来的学习中，请尝试用 AI 解决以下问题：

### 场景 1：代码解释 (Explain)
遇到看不懂的源码，不要发呆，直接选中代码，询问通义灵码：
> *"Please explain this code block step by step. (请一步步解释这段代码是做什么的)"*

### 场景 2：生成样板代码 (Generate)
实体类的 Getter/Setter 也就是眨眼间的事，更高级的是生成单元测试：
> *"请为当前的 `UserService` 类生成 JUnit 5 单元测试，覆盖成功登录和密码错误两种情况。"*

### 场景 3：辅助 Debug (Fix)
控制台报错了？直接把红色的 Exception 堆栈信息复制给 DeepSeek：
> *"我遇到了这个 `NullPointerException`，这是我的报错日志和相关代码，请分析原因并给出修改建议。"*

---

## ⚠️ 警示：做驾驶员，而不是乘客

AI 很强，但它也会一本正经地胡说八道（幻觉）。

!!! warning "使用的底线"
    1.  **不可直接复制粘贴**：AI 生成的代码必须经由你阅读、理解并测试通过后，才能提交到 Git。
    2.  **拒绝思维懒惰**：AI 只是副驾 (Co-pilot)，你才是主驾 (Pilot)。**系统的业务逻辑、安全性与架构设计，必须由你掌控。**
    3.  **隐私保护**：不要将真实的服务器密码、私钥等敏感信息发送给公有云 AI。

---

## 🧪 准备好开始实验了吗？

现在你已经了解了工具和方法，接下来让我们在 **实验 1** 中亲手配置它们，并完成你的第一次 AI 辅助编程体验。

[进入实验 1：Web环境与AI助手配置 >>](lab1.md){ .md-button .md-button--primary }